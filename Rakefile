# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/valar.rb'

Hoe.new('valar', Valar::VERSION) do |p|
  # p.rubyforge_name = 'valarx' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
